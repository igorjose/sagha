$(document).on 'ready page:load', ->
  $("#servidor_arquitetura").selectpicker({style: 'btn-primary', menuStyle: 'dropdown-inverse'});
  $("#servidor_servicos").selectpicker({style: 'btn-primary', menuStyle: 'dropdown-inverse'});
